{  
    // 这里需要根据不同语言进行修改后缀，这里是 .js,如果是其他语言只要把js改成例如 lua、php等
    "scope": "source.json - keyword.control.json - constant.language.json - string",

    "completions":
    [
        { "trigger": "trigger - completions", "contents": "{ \"trigger\": \"$0 | 2.0引擎\", \"contents\": \"$0\" }," },
    ]
}